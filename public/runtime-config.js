/* Runtime knobs for the static map. Overwritten by the pod entrypoint when
 * PUBLIC_MODE / PUBLIC_FULL_LIVE are set. Build-time VITE_PUBLIC_* still apply
 * when these keys are left undefined. */
window.__MIRMICODE__ = window.__MIRMICODE__ || {};
